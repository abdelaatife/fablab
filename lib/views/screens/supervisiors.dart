import 'package:flutter/material.dart';

class Supervisiors extends StatelessWidget {
  const Supervisiors({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}